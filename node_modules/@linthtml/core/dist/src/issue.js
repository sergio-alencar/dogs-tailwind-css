export const ISSUE_SEVERITY = {
    ERROR: "error",
    WARNING: "warning"
};
export default class Issue {
    // TODO: CHECK why options.rule (legacy, need to be removed)
    constructor(rule_name, position, options) {
        var _a;
        this.data = {};
        this.severity = ISSUE_SEVERITY.ERROR;
        this.position = position;
        this.code = options.code;
        this.rule = rule_name;
        this.message = (_a = options.message) !== null && _a !== void 0 ? _a : "";
        this.data = options.data || {};
        this.severity = options.severity || ISSUE_SEVERITY.ERROR;
    }
}
