import { is_tag_node } from "@linthtml/dom-utils";
const RULE_NAME = "fieldset-contains-legend";
function lint(element, _config, { report }) {
    if (is_tag_node(element) && element.name === "fieldset") {
        const has_legend = element.children.some((node) => node.name === "legend");
        if (has_legend === false) {
            report({
                code: "E063",
                position: element.open.loc
            });
        }
    }
}
export default {
    name: RULE_NAME,
    lint
};
